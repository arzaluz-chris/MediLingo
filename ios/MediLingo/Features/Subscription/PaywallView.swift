import SwiftUI

// Premium paywall (CLAUDE-ios.md § Subscription). Placeholder; StoreKit in Phase 3.
struct PaywallView: View {
    var body: some View {
        FeatureStub(title: "MediLingo Premium", systemImage: "crown.fill",
                    subtitle: "Cursos ilimitados, IA y modo sin conexión.")
    }
}
