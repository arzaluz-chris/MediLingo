import SwiftUI

// Course tree / learning path (CLAUDE-ios.md § Learning). Placeholder for Phase 0.
struct LearningView: View {
    var body: some View {
        FeatureStub(title: "Aprender", systemImage: "book.fill",
                    subtitle: "Aquí verás tus cursos y lecciones.")
    }
}
