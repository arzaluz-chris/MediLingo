import SwiftUI

// Friends / leaderboard / leagues (CLAUDE-ios.md § Social). Placeholder.
struct SocialView: View {
    var body: some View {
        FeatureStub(title: "Social", systemImage: "person.3.fill",
                    subtitle: "Amigos, tabla de líderes y ligas.")
    }
}
