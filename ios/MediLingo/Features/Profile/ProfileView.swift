import SwiftUI

// User profile + stats (CLAUDE-ios.md § Profile & Stats). Placeholder for Phase 0.
struct ProfileView: View {
    var body: some View {
        FeatureStub(title: "Perfil", systemImage: "person.fill",
                    subtitle: "Tus estadísticas, logros y racha.")
    }
}
